# businessanalytics

Used packages for Python:
-- tkinter
-- psycopg2

Used packages for R:
-- shiny
-- DBI
-- RPostgres
-- ggplot2

This repository has two frontend applications.
frontend_without_login.py | A complete version in which a user can navigate and edit in all tabs
frontend_login_version.py | A version that has a login functionality, restriction user to only the intended feature
